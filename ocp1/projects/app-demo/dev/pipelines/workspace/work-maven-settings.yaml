apiVersion: v1
kind: ConfigMap
metadata:
  name: maven-settings
data:
  settings.xml: |
    <settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0
                              http://maven.apache.org/xsd/settings-1.0.0.xsd">

      <!-- Credenciales para autenticación -->
      <servers>
          <server>
              <id>nexus-sancor</id>
              <username>deployer</username>
              <password>S4nc0rS4lud</password>
          </server>
      </servers>

      <mirrors>
          <mirror>
              <id>nexus</id>
              <mirrorOf>*</mirrorOf>
              <url>http://nexus-nexus.apps.ams01-noprod.ams.red/repository/maven-public/</url>
          </mirror>
      </mirrors>
    </settings>